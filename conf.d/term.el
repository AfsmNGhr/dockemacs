;; ================================ Shell ======================================

(use-package shell :defer t
  :config
  (setq explicit-shell-file-name "/bin/bash"))

;; ========================= To be continued... ================================
