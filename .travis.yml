language: emacs-lisp

env:
  matrix:
    - EMACS=emacs24

cache:
  directories:
    - ${HOME}/.emacs.d/elpa

install:
  - sudo add-apt-repository -y ppa:cassou/emacs
  - sudo apt-get update -qq
  - sudo apt-get install ${EMACS} -qq
  - export HOME=$PWD/..
  - ln -s emacs.d ../.emacs.d
  - mkdir -p ../emacs.d/conf.d
  - ln -s emacs.d/conf.d ../emacs.d/conf.d

script:
  - ${EMACS} --version
  - ./test-startup.el